# คำสั่งทั้งหมดสำหรับสร้างและใช้งานเว็บไซต์

เอกสารนี้รวบรวมคำสั่งทั้งหมดที่ใช้ในการสร้างและพัฒนาเว็บไซต์ Next.js นี้ พร้อมคำอธิบายการใช้งาน

---

## 📋 สารบัญ

1. [การติดตั้งโปรเจกต์](#การติดตั้งโปรเจกต์) - คำสั่งสำหรับติดตั้ง dependencies และตั้งค่าโปรเจกต์
2. [การตั้งค่าฐานข้อมูล](#การตั้งค่าฐานข้อมูล) - คำสั่งสำหรับสร้างฐานข้อมูลและ import schema
3. [การตั้งค่า Environment Variables](#การตั้งค่า-environment-variables) - วิธีตั้งค่าไฟล์ .env.local และ environment variables
4. [คำสั่งสำหรับ Development](#คำสั่งสำหรับ-development) - คำสั่งสำหรับรัน development server และ lint
5. [คำสั่งสำหรับ Build และ Production](#คำสั่งสำหรับ-build-และ-production) - คำสั่งสำหรับ build และรัน production server
6. [คำสั่งสำหรับ Database](#คำสั่งสำหรับ-database) - คำสั่งสำหรับเชื่อมต่อ, backup และ restore ฐานข้อมูล
7. [คำสั่งสำหรับ Git](#คำสั่งสำหรับ-git) - คำสั่ง Git สำหรับจัดการ version control
8. [คำสั่งสำหรับ TypeScript](#คำสั่งสำหรับ-typescript) - คำสั่ง TypeScript สำหรับตรวจสอบ type errors

---

## 🚀 การติดตั้งโปรเจกต์

### 1. ติดตั้ง Dependencies

```bash
npm install
```

**คำอธิบาย:** 
- ติดตั้งแพ็กเกจทั้งหมดที่ระบุใน `package.json` รวมถึง dependencies และ devDependencies
- สร้างโฟลเดอร์ `node_modules` และไฟล์ `package-lock.json`
- ควรรันคำสั่งนี้เมื่อเริ่มต้นโปรเจกต์หรือเมื่อมีการเพิ่มแพ็กเกจใหม่

**เมื่อไหร่ควรใช้:**
- เมื่อ clone โปรเจกต์มาใหม่
- เมื่อมีการอัปเดต `package.json`
- เมื่อต้องการติดตั้ง dependencies ใหม่ทั้งหมด

---

### 2. ติดตั้ง Dependencies แบบ Clean (ลบ node_modules ก่อน)

```bash
rm -rf node_modules package-lock.json
npm install
```

**คำอธิบาย:**
- ลบโฟลเดอร์ `node_modules` และไฟล์ `package-lock.json` ก่อน
- จากนั้นติดตั้ง dependencies ใหม่ทั้งหมด
- ใช้เมื่อมีปัญหาเกี่ยวกับ dependencies หรือต้องการเริ่มต้นใหม่

**สำหรับ Windows PowerShell:**
```powershell
Remove-Item -Recurse -Force node_modules, package-lock.json
npm install
```

---

## 🗄️ การตั้งค่าฐานข้อมูล

### 1. สร้างฐานข้อมูล MySQL

```bash
mysql -u root -p
```

**คำอธิบาย:**
- เข้าสู่ระบบ MySQL command line
- ต้องมี MySQL Server ติดตั้งอยู่บนเครื่อง
- จะถามรหัสผ่านของ MySQL root user

**หลังจากเข้าสู่ระบบแล้ว:**
```sql
CREATE DATABASE punchvv CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE punchvv;
```

**คำอธิบาย:**
- สร้างฐานข้อมูลชื่อ `punchvv` พร้อมตั้งค่า character set เป็น utf8mb4
- เลือกใช้ฐานข้อมูล `punchvv`

---

### 2. นำเข้าสคีมาฐานข้อมูลหลัก

```bash
mysql -u punchmade -p punchvv < perfume.sql
```

**คำอธิบาย:**
- นำเข้าสคีมาฐานข้อมูลจากไฟล์ `perfume.sql`
- สร้างตารางหลักๆ เช่น `users`, `products`, `orders`, `order_items`, `reviews`
- ใช้ username `punchmade` (หรือเปลี่ยนตามที่ตั้งค่า)

**หรือใช้คำสั่งใน MySQL:**
```sql
SOURCE perfume.sql;
```

---

### 3. นำเข้าสคีมาสำหรับ Slip Verification

```bash
mysql -u punchmade -p punchvv < lib/slip-schema.sql
```

**คำอธิบาย:**
- นำเข้าสคีมาสำหรับระบบตรวจสอบสลิปโอนเงิน
- สร้างตาราง `slip_verifications` และ `slip_accounts`
- ต้องรันหลังจากสร้างตารางหลักแล้ว

**หรือใช้คำสั่งใน MySQL:**
```sql
SOURCE lib/slip-schema.sql;
```

---

## ⚙️ การตั้งค่า Environment Variables

### 1. สร้างไฟล์ .env.local

```bash
cp .env.example .env.local
```

**คำอธิบาย:**
- คัดลอกไฟล์ `.env.example` เป็น `.env.local`
- ไฟล์ `.env.local` จะถูก git ignore และใช้สำหรับเก็บค่าคอนฟิกเฉพาะเครื่อง

**สำหรับ Windows PowerShell:**
```powershell
Copy-Item .env.example .env.local
```

---

### 2. แก้ไขไฟล์ .env.local

เปิดไฟล์ `.env.local` และกรอกค่าตามที่ต้องการ:

```env
# Database Configuration
DB_HOST=210.246.215.19
DB_PORT=3306
DB_USER=punchmade
DB_PASSWORD=your_password_here
DB_NAME=punchvv

# JWT Secret สำหรับ login
JWT_SECRET=2548820c90ba94fbf0b6020bed0dff78

# pic.in.th API key สำหรับอัพโหลดรูป
PICINTH_API_KEY=your_pic_in_th_api_key

# Slip Verify API credentials
SLIP_VERIFY_CLIENT_ID=en78zieunia09j1t
SLIP_VERIFY_CLIENT_SECRET=h77coiaar1mg1nrgrvnvdcrt497u4tof
```

**คำอธิบาย:**
- `DB_HOST`: ที่อยู่ IP หรือ hostname ของ MySQL server
- `DB_PORT`: พอร์ตของ MySQL (default: 3306)
- `DB_USER`: username สำหรับเชื่อมต่อฐานข้อมูล
- `DB_PASSWORD`: รหัสผ่านสำหรับเชื่อมต่อฐานข้อมูล
- `DB_NAME`: ชื่อฐานข้อมูล
- `JWT_SECRET`: Secret key สำหรับสร้าง JWT token (ควรเปลี่ยนเป็นค่าที่ปลอดภัย)
- `PICINTH_API_KEY`: API key สำหรับอัพโหลดรูปภาพไปยัง pic.in.th
- `SLIP_VERIFY_CLIENT_ID` และ `SLIP_VERIFY_CLIENT_SECRET`: Credentials สำหรับ API ตรวจสอบสลิป

**หมายเหตุ:** หากไม่ตั้งค่า environment variables บางตัว โปรแกรมจะใช้ค่า default ที่กำหนดไว้ในโค้ด

---

## 💻 คำสั่งสำหรับ Development

### 1. รัน Development Server

```bash
npm run dev
```

**คำอธิบาย:**
- เริ่มต้น Next.js development server พร้อม Turbo mode
- เว็บไซต์จะรันที่ `http://localhost:3000`
- มี hot reload อัตโนมัติเมื่อมีการแก้ไขโค้ด
- ใช้ Turbo mode เพื่อเพิ่มความเร็วในการ build

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการพัฒนาและทดสอบโค้ด
- เมื่อต้องการดูการเปลี่ยนแปลงแบบ real-time

**การหยุดการทำงาน:**
- กด `Ctrl + C` ใน terminal

---

### 2. รัน Linter เพื่อตรวจสอบโค้ด

```bash
npm run lint
```

**คำอธิบาย:**
- ตรวจสอบโค้ดด้วย ESLint
- แสดงข้อผิดพลาดและคำเตือนเกี่ยวกับ code style และ best practices
- ใช้ Next.js ESLint config

**เมื่อไหร่ควรใช้:**
- ก่อน commit โค้ด
- เมื่อต้องการตรวจสอบคุณภาพโค้ด
- เมื่อต้องการหาปัญหาในโค้ด

---

## 🏗️ คำสั่งสำหรับ Build และ Production

### 1. Build โปรเจกต์สำหรับ Production

```bash
npm run build
```

**คำอธิบาย:**
- Build โปรเจกต์ Next.js สำหรับ production
- สร้าง optimized production build ในโฟลเดอร์ `.next`
- ตรวจสอบ TypeScript errors และ warnings
- Optimize images, CSS, และ JavaScript
- ใช้เวลาสักครู่ในการ build ขึ้นอยู่กับขนาดโปรเจกต์

**เมื่อไหร่ควรใช้:**
- ก่อน deploy เว็บไซต์
- เมื่อต้องการทดสอบ production build
- เมื่อต้องการตรวจสอบว่าโค้ดสามารถ build ได้หรือไม่

**หมายเหตุ:** ต้องแก้ไข errors ทั้งหมดก่อน build สำเร็จ

---

### 2. รัน Production Server

```bash
npm run start
```

**คำอธิบาย:**
- รัน Next.js production server
- ต้อง build โปรเจกต์ก่อนด้วย `npm run build`
- เว็บไซต์จะรันที่ `http://localhost:3000`
- ไม่มี hot reload (ใช้ production build)

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการทดสอบ production build บนเครื่อง
- เมื่อต้องการจำลองสภาพแวดล้อม production

**หมายเหตุ:** ไม่ควรใช้คำสั่งนี้ใน development ควรใช้ `npm run dev` แทน

---

## 🗃️ คำสั่งสำหรับ Database

### 1. เชื่อมต่อฐานข้อมูล MySQL

```bash
mysql -u punchmade -p punchvv
```

**คำอธิบาย:**
- เชื่อมต่อฐานข้อมูล MySQL โดยใช้ username `punchmade`
- จะถามรหัสผ่าน
- เลือกใช้ฐานข้อมูล `punchvv` ทันที

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการดูหรือแก้ไขข้อมูลในฐานข้อมูล
- เมื่อต้องการรัน SQL queries โดยตรง

---

### 2. Export ฐานข้อมูล (Backup)

```bash
mysqldump -u punchmade -p punchvv > backup.sql
```

**คำอธิบาย:**
- Export ฐานข้อมูลทั้งหมดเป็นไฟล์ SQL
- สร้างไฟล์ `backup.sql` ที่มีข้อมูลและโครงสร้างทั้งหมด
- ใช้สำหรับ backup ฐานข้อมูล

**เมื่อไหร่ควรใช้:**
- ก่อนทำการเปลี่ยนแปลงสำคัญ
- เมื่อต้องการ backup ข้อมูล
- เมื่อต้องการย้ายฐานข้อมูลไปยัง server อื่น

---

### 3. Import ฐานข้อมูล (Restore)

```bash
mysql -u punchmade -p punchvv < backup.sql
```

**คำอธิบาย:**
- นำเข้าข้อมูลจากไฟล์ SQL backup
- **คำเตือน:** จะเขียนทับข้อมูลที่มีอยู่ทั้งหมด

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการ restore จาก backup
- เมื่อต้องการย้ายข้อมูลไปยัง server ใหม่

---

## 📦 คำสั่งสำหรับ Package Management

### 1. เพิ่มแพ็กเกจใหม่

```bash
npm install package-name
```

**คำอธิบาย:**
- ติดตั้งแพ็กเกจใหม่และเพิ่มเข้าไปใน `dependencies`
- ตัวอย่าง: `npm install axios`

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการใช้ library หรือ package ใหม่

---

### 2. เพิ่มแพ็กเกจสำหรับ Development

```bash
npm install -D package-name
```

**คำอธิบาย:**
- ติดตั้งแพ็กเกจใหม่และเพิ่มเข้าไปใน `devDependencies`
- ใช้สำหรับ tools ที่ใช้เฉพาะตอน development
- ตัวอย่าง: `npm install -D @types/node`

---

### 3. ลบแพ็กเกจ

```bash
npm uninstall package-name
```

**คำอธิบาย:**
- ลบแพ็กเกจออกจากโปรเจกต์และ `package.json`

---

### 4. อัปเดตแพ็กเกจทั้งหมด

```bash
npm update
```

**คำอธิบาย:**
- อัปเดตแพ็กเกจทั้งหมดให้เป็นเวอร์ชันล่าสุดที่เข้ากันได้กับ `package.json`

---

### 5. ตรวจสอบแพ็กเกจที่ล้าสมัย

```bash
npm outdated
```

**คำอธิบาย:**
- แสดงรายการแพ็กเกจที่มีเวอร์ชันใหม่กว่าที่ติดตั้งอยู่

---

## 🔧 คำสั่งสำหรับ Git

### 1. Clone โปรเจกต์

```bash
git clone <repository-url>
cd webpra-frontend-nextjs
```

**คำอธิบาย:**
- ดาวน์โหลดโปรเจกต์จาก Git repository
- เข้าไปในโฟลเดอร์โปรเจกต์

---

### 2. ตรวจสอบสถานะ Git

```bash
git status
```

**คำอธิบาย:**
- แสดงไฟล์ที่เปลี่ยนแปลง, เพิ่มใหม่, หรือถูกลบ
- แสดง branch ปัจจุบัน

---

### 3. เพิ่มไฟล์เข้า Staging Area

```bash
git add .
```

**คำอธิบาย:**
- เพิ่มไฟล์ทั้งหมดที่เปลี่ยนแปลงเข้า staging area
- หรือใช้ `git add <filename>` เพื่อเพิ่มไฟล์เฉพาะ

---

### 4. Commit การเปลี่ยนแปลง

```bash
git commit -m "ข้อความอธิบายการเปลี่ยนแปลง"
```

**คำอธิบาย:**
- บันทึกการเปลี่ยนแปลงพร้อมข้อความอธิบาย
- ควรเขียน commit message ที่ชัดเจนและอธิบายสิ่งที่เปลี่ยนแปลง

---

### 5. Push ไปยัง Remote Repository

```bash
git push origin main
```

**คำอธิบาย:**
- ส่งการเปลี่ยนแปลงไปยัง remote repository
- `main` คือชื่อ branch (อาจเป็น `master` หรือชื่ออื่น)

---

### 6. Pull การเปลี่ยนแปลงล่าสุด

```bash
git pull origin main
```

**คำอธิบาย:**
- ดาวน์โหลดและ merge การเปลี่ยนแปลงล่าสุดจาก remote repository

---

## 🔷 คำสั่งสำหรับ TypeScript

โปรเจกต์นี้ใช้ TypeScript เป็นภาษาหลัก Next.js จะจัดการ TypeScript compilation และ type checking อัตโนมัติ แต่คุณสามารถใช้คำสั่ง TypeScript โดยตรงได้ดังนี้:

### 1. ตรวจสอบ Type Errors โดยไม่ Build

```bash
npx tsc --noEmit
```

**คำอธิบาย:**
- ตรวจสอบ type errors ทั้งหมดในโปรเจกต์โดยไม่สร้างไฟล์ output
- ใช้การตั้งค่าจาก `tsconfig.json`
- แสดง errors และ warnings ทั้งหมดที่พบ
- ไม่สร้างไฟล์ `.js` หรือ `.d.ts` (เพราะ `noEmit: true` ใน tsconfig.json)

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการตรวจสอบ type errors ก่อน commit
- เมื่อต้องการตรวจสอบว่าโค้ด TypeScript ถูกต้องหรือไม่
- เมื่อต้องการดู type errors โดยไม่ต้อง build ทั้งโปรเจกต์

**ตัวอย่าง Output:**
```
app/page.tsx:10:5 - error TS2322: Type 'string' is not assignable to type 'number'.
```

---

### 2. ตรวจสอบ Type Errors แบบ Watch Mode

```bash
npx tsc --noEmit --watch
```

**คำอธิบาย:**
- ตรวจสอบ type errors แบบ watch mode
- จะตรวจสอบใหม่ทุกครั้งที่มีการเปลี่ยนแปลงไฟล์
- กด `Ctrl + C` เพื่อหยุดการทำงาน

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการตรวจสอบ type errors แบบ real-time ขณะพัฒนา
- เมื่อต้องการดู type errors ทันทีที่แก้ไขโค้ด

---

### 3. ตรวจสอบ Type Errors ในไฟล์เฉพาะ

```bash
npx tsc --noEmit app/page.tsx
```

**คำอธิบาย:**
- ตรวจสอบ type errors ในไฟล์หรือโฟลเดอร์ที่ระบุเท่านั้น
- ใช้ได้กับไฟล์เดียวหรือหลายไฟล์

**ตัวอย่าง:**
```bash
npx tsc --noEmit app/**/*.tsx
npx tsc --noEmit lib/utils.ts
```

---

### 4. ตรวจสอบ Type Errors พร้อมแสดงรายละเอียด

```bash
npx tsc --noEmit --pretty
```

**คำอธิบาย:**
- ตรวจสอบ type errors พร้อมแสดงผลลัพธ์แบบสวยงาม (สีและ formatting)
- ใช้สีและ formatting เพื่อให้อ่านง่ายขึ้น

---

### 5. ตรวจสอบ Type Errors แบบแสดงเฉพาะ Errors (ไม่แสดง Warnings)

```bash
npx tsc --noEmit --pretty false
```

**คำอธิบาย:**
- ตรวจสอบ type errors โดยไม่แสดง warnings
- แสดงเฉพาะ errors ที่ร้ายแรงเท่านั้น

---

### 6. ตรวจสอบ Type Errors พร้อมแสดงสถิติ

```bash
npx tsc --noEmit --listFiles
```

**คำอธิบาย:**
- ตรวจสอบ type errors พร้อมแสดงรายชื่อไฟล์ทั้งหมดที่ถูกตรวจสอบ
- มีประโยชน์เมื่อต้องการดูว่าไฟล์ไหนถูกตรวจสอบบ้าง

---

### 7. Build TypeScript (ถ้าต้องการ)

```bash
npx tsc
```

**คำอธิบาย:**
- Compile TypeScript เป็น JavaScript
- **หมายเหตุ:** ในโปรเจกต์นี้ `tsconfig.json` มี `noEmit: true` ดังนั้นคำสั่งนี้จะไม่สร้างไฟล์ output
- Next.js จะจัดการ compilation ให้อัตโนมัติเมื่อรัน `npm run dev` หรือ `npm run build`

**เมื่อไหร่ควรใช้:**
- เมื่อต้องการทดสอบ TypeScript compiler โดยตรง
- เมื่อต้องการดูว่า TypeScript สามารถ compile ได้หรือไม่

---

### 8. ตรวจสอบ Type Errors แบบ Incremental

```bash
npx tsc --noEmit --incremental
```

**คำอธิบาย:**
- ใช้ incremental compilation เพื่อเพิ่มความเร็ว
- สร้างไฟล์ `.tsbuildinfo` เพื่อเก็บข้อมูลการ compile
- ตรวจสอบเฉพาะไฟล์ที่เปลี่ยนแปลง

**หมายเหตุ:** `tsconfig.json` มี `incremental: true` อยู่แล้ว ดังนั้นคำสั่งนี้จะใช้การตั้งค่านั้น

---

### 9. ตรวจสอบ Type Errors แบบ Strict

```bash
npx tsc --noEmit --strict
```

**คำอธิบาย:**
- ตรวจสอบ type errors แบบ strict mode
- **หมายเหตุ:** `tsconfig.json` มี `strict: true` อยู่แล้ว ดังนั้นคำสั่งนี้จะใช้การตั้งค่านั้น

---

### 10. ตรวจสอบ Type Errors พร้อมแสดง Help

```bash
npx tsc --help
```

**คำอธิบาย:**
- แสดงรายการ options ทั้งหมดที่ใช้ได้กับ TypeScript compiler
- มีประโยชน์เมื่อต้องการดู options อื่นๆ ที่สามารถใช้ได้

---

### 11. ตรวจสอบเวอร์ชัน TypeScript

```bash
npx tsc --version
```

**คำอธิบาย:**
- แสดงเวอร์ชันของ TypeScript ที่ติดตั้งในโปรเจกต์
- ตัวอย่าง output: `Version 5.9.3`

---

### 12. ตรวจสอบ Type Errors ในโฟลเดอร์เฉพาะ

```bash
npx tsc --noEmit app/
npx tsc --noEmit lib/
npx tsc --noEmit components/
```

**คำอธิบาย:**
- ตรวจสอบ type errors ในโฟลเดอร์ที่ระบุเท่านั้น
- มีประโยชน์เมื่อต้องการตรวจสอบเฉพาะส่วนหนึ่งของโปรเจกต์

---

### 13. ตรวจสอบ Type Errors พร้อมแสดง Project References

```bash
npx tsc --noEmit --build
```

**คำอธิบาย:**
- ตรวจสอบ type errors พร้อม build project references (ถ้ามี)
- ใช้เมื่อโปรเจกต์มี project references

---

### 14. ตรวจสอบ Type Errors แบบแสดง Source Map

```bash
npx tsc --noEmit --sourceMap
```

**คำอธิบาย:**
- ตรวจสอบ type errors พร้อมสร้าง source map (ถ้าไม่ใช่ noEmit)
- ในกรณีนี้จะไม่สร้างไฟล์เพราะมี `--noEmit`

---

### 15. ตรวจสอบ Type Errors แบบแสดง Declaration Files

```bash
npx tsc --noEmit --declaration
```

**คำอธิบาย:**
- ตรวจสอบ type errors พร้อมสร้าง declaration files (`.d.ts`)
- ในกรณีนี้จะไม่สร้างไฟล์เพราะมี `--noEmit`

---

## 📝 TypeScript ใน Next.js

### Type Checking อัตโนมัติ

Next.js จะทำ type checking อัตโนมัติเมื่อ:

1. **Development Mode (`npm run dev`):**
   - TypeScript จะถูก compile และ type-check แบบ real-time
   - แสดง errors ใน terminal และ browser
   - มี hot reload เมื่อแก้ไขโค้ด

2. **Build Mode (`npm run build`):**
   - TypeScript จะถูก compile และ type-check ก่อน build
   - ถ้ามี type errors จะหยุดการ build และแสดง errors
   - ต้องแก้ไข errors ทั้งหมดก่อน build สำเร็จ

### การตั้งค่า TypeScript

การตั้งค่า TypeScript อยู่ในไฟล์ `tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

**คำอธิบายการตั้งค่าสำคัญ:**
- `strict: true` - เปิด strict type checking
- `noEmit: true` - ไม่สร้างไฟล์ output (Next.js จะจัดการเอง)
- `jsx: "react-jsx"` - ใช้ React JSX transform
- `paths: { "@/*": ["./*"] }` - ตั้งค่า path aliases สำหรับ import

### Type Definitions ที่ใช้ในโปรเจกต์

โปรเจกต์นี้ใช้ type definitions ต่อไปนี้:

- `@types/node` - Type definitions สำหรับ Node.js
- `@types/react` - Type definitions สำหรับ React
- `@types/react-dom` - Type definitions สำหรับ React DOM
- `@types/bcryptjs` - Type definitions สำหรับ bcryptjs

---

## 📝 สรุปคำสั่งที่ใช้บ่อยที่สุด

### สำหรับการเริ่มต้นโปรเจกต์ใหม่:

```bash
# 1. Clone หรือดาวน์โหลดโปรเจกต์
git clone <repository-url>
cd webpra-frontend-nextjs

# 2. ติดตั้ง dependencies
npm install

# 3. สร้างไฟล์ environment
cp .env.example .env.local
# แก้ไข .env.local ตามต้องการ

# 4. สร้างและตั้งค่าฐานข้อมูล
mysql -u root -p
# CREATE DATABASE punchvv;
mysql -u punchmade -p punchvv < perfume.sql
mysql -u punchmade -p punchvv < lib/slip-schema.sql

# 5. รัน development server
npm run dev
```

### สำหรับการพัฒนาในแต่ละวัน:

```bash
# เริ่ม development server
npm run dev

# ตรวจสอบโค้ด
npm run lint

# ตรวจสอบ TypeScript errors
npx tsc --noEmit

# Build สำหรับ production
npm run build
```

### สำหรับการตรวจสอบ TypeScript:

```bash
# ตรวจสอบ type errors ทั้งโปรเจกต์
npx tsc --noEmit

# ตรวจสอบ type errors แบบ watch mode
npx tsc --noEmit --watch

# ตรวจสอบ type errors ในไฟล์เฉพาะ
npx tsc --noEmit app/page.tsx
```

---

## ⚠️ หมายเหตุสำคัญ

1. **Environment Variables:** อย่าลืมสร้างไฟล์ `.env.local` และตั้งค่าตามที่ต้องการ
2. **Database:** ต้องมี MySQL server รันอยู่และสร้างฐานข้อมูลก่อน
3. **Port:** Default port คือ 3000 หากถูกใช้งานแล้ว Next.js จะใช้ port ถัดไปอัตโนมัติ
4. **Node.js Version:** ควรใช้ Node.js เวอร์ชันที่รองรับ Next.js 16
5. **TypeScript:** โปรเจกต์นี้ใช้ TypeScript ต้องตรวจสอบ type errors ก่อน build

---

## 🆘 การแก้ไขปัญหาที่พบบ่อย

### ปัญหา: Port 3000 ถูกใช้งานแล้ว

**วิธีแก้:**
```bash
# ใช้ port อื่น
npm run dev -- -p 3001
```

### ปัญหา: Cannot connect to database

**วิธีแก้:**
- ตรวจสอบว่า MySQL server รันอยู่หรือไม่
- ตรวจสอบ credentials ใน `.env.local`
- ตรวจสอบว่า firewall อนุญาตการเชื่อมต่อหรือไม่

### ปัญหา: Module not found

**วิธีแก้:**
```bash
# ลบ node_modules และติดตั้งใหม่
rm -rf node_modules package-lock.json
npm install
```

### ปัญหา: TypeScript errors เยอะมาก

**วิธีแก้:**
```bash
# ตรวจสอบ type errors แบบแสดงเฉพาะ errors
npx tsc --noEmit --pretty false

# หรือตรวจสอบทีละไฟล์
npx tsc --noEmit app/page.tsx
```

### ปัญหา: TypeScript ไม่รู้จัก types จาก package

**วิธีแก้:**
```bash
# ติดตั้ง type definitions ที่ขาดหายไป
npm install -D @types/package-name

# หรือตรวจสอบว่า skipLibCheck เปิดอยู่หรือไม่ใน tsconfig.json
```

---

**อัปเดตล่าสุด:** กุมภาพันธ์ 2026
